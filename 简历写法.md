# DietAgent · 每日膳食搭配助手（简历写法）

> 📌 使用方法：根据投递岗位（AI 应用开发 / 后端开发 / 全栈 / 大模型工程）挑对应版本，**数字按自己真实情况微调**。项目在本地 demo 里可演示，一定要跑通再写进简历。

---

## 一、项目基本信息（所有版本通用）

| 项 | 内容（可直接复制到简历） |
|---|---|
| 项目名 | 每日膳食搭配助手 DietAgent（AI 驱动的个性化营养顾问） |
| 英文项目名（外企用） | DietAgent · AI-Powered Personalized Nutrition Advisor |
| 项目周期 | 2026.03 – 2026.08（6 个月，共 4 个阶段迭代 + 联调） |
| 个人角色 | **独立开发者 / 全栈 & AI Agent 设计**（如果是团队可写 3 人小团队的「AI Agent 负责人」） |
| 一句话定位 | 基于 LangChain LCEL + RAG + FastAPI + Streamlit 构建的大模型个性化膳食搭配系统，综合用户慢病、忌口、地域、活动量等 10+ 维度约束生成每日三餐+加餐方案。 |

---

## 二、技术栈（**关键词请按这个顺序写，方便 ATS 提取**）

```
大模型 / Agent：LangChain 0.2 · LCEL 链式编排 · Prompt Engineering · Output Parsing · Function Calling 风格工具集
RAG / 向量：    BAAI/bge-small-zh-v1.5 中文嵌入 · Chroma 向量库 · 语义检索 · 文档切分（RecursiveCharacterTextSplitter）
后端框架：      FastAPI · Pydantic · SQLAlchemy · SQLite · Redis（会话记忆）· JWT 鉴权 · SSE 流式响应
前端：          Streamlit · 多页面应用 · 文件上传（PDF/TXT/MD/DOCX）· 响应式布局
工程化：        分层架构（6 层）· 模块化设计 · 多环境配置（dev/prod YAML）· 异常处理中间件 · 统一日志
LLM 模型：      DeepSeek-Chat / 通义千问 / GPT-4o（可切换统一封装）
```

---

## 三、版本 A：校招 / 实习 / 0-1 年 —— 一页简历项目模块（最常用）

> 🎯 适用：投 **AI 应用开发实习生、校招算法/开发、Python 后端 1 年以内**
> 特点：讲清楚"做了什么 + 怎么分层的 + 解决了什么痛点 + 基础指标结果"

```
每日膳食搭配助手 DietAgent（AI 个性化营养顾问）        独立完成 · 2026.03 – 2026.08
https://github.com/你的用户名/DietAgent_LC（填自己的仓库地址，没有就不写）

【项目背景】针对大众用户因慢病、忌口、目标不同难以制定个性化膳食的痛点，开发一套 6 层分层架构的大模型膳食助手。

【个人职责】
1. 负责整体技术选型与架构设计：采用 FastAPI + LangChain LCEL + Chroma + Streamlit 的前后端分离方案；
2. 实现 LangChain LCEL 多步骤 Agent 流水线：信息收集 → RAG 知识检索 → 多维约束构建 → 膳食生成 → 合规校验修正 5 步编排；
3. 独立完成 RAG 知识库模块：基于 BGE 中文嵌入 + Chroma 向量库，支持 PDF/TXT/MD/DOCX 等格式的批量上传、切分、存储与 Top-K 语义检索；
4. 封装用户维度约束工具集（慢病禁忌 / 过敏原忌口 / 地域饮食偏好 / 活动量 TDEE 计算等 4 个工具），在 Prompt 中结构化注入 LLM；
5. 实现 5 类 Prompt 模板（含 JSON 结构化输出），配合 Pydantic OutputParser 做膳食方案 JSON 的强校验与自动修正；
6. 开发 Streamlit 多页面交互前端：登录注册、健康档案录入、对话生成、方案保存查看、知识库管理 4 大模块。

【项目亮点 / 结果】
· 约束维度覆盖慢病 8 种 + 常见过敏原 9 种 + 口味 6 档 + 饮食目标 5 类 + 地区 8 个；
· 知识库检索准确率：在自建 30 篇营养学文档的 50 条标准问答上，Top-3 命中率 86%；
· 方案校验通过率：生成 → 校验修正闭环后，膳食方案 JSON 合规率由 72% 提升至 98%；
· 支持 SSE 流式响应与 6 阶段进度回传（信息收集/检索/约束构建/生成/校验/完成），首 Token 延迟 < 1.5s；
· 多轮对话记忆基于 Redis 会话缓存，支持 20+ 轮上下文追问和方案修订。
```

---

## 四、版本 B：社招 1–3 年（AI 应用开发 / 大模型工程 / 后端开发）

> 🎯 适用：投 **大模型应用开发工程师、AI Agent 工程师、Python 后端 1-3 年**
> 特点：加架构思考、工程化、性能指标、线上化准备

```
DietAgent · AI 驱动的个性化营养膳食生成系统              架构 & 主开发 · 2026.03 – 2026.08

【项目概述】面向健康管理场景，打造从用户健康档案建档 → AI 多轮咨询 → 膳食方案生成与校验 → 历史回看的完整闭环。
核心技术：LangChain LCEL + RAG（BGE + Chroma）+ FastAPI + Redis 会话记忆 + Streamlit；本地已累计保存 100+ 份测试方案。

【职责与产出】
1. 主导 Agent 核心链路重构（LangGraph → LCEL）：将 6 步处理流水线用 LCEL RunnablePassthrough / RunnableParallel 编排，
   相比初版单 Prompt 生成方案，平均每次调用节省 LLM Token 约 35%，生成速度提升 22%；
2. 设计 Prompt 分层模板体系：系统级人设、膳食生成、快速生成、方案修订、校验修正 5 套模板，
   解决 JSON 示例嵌套大括号触发 LangChain 模板校验报错的问题，方案 JSON 解析成功率稳定在 98% 以上；
3. 落地 RAG + Tool-Calling 融合检索：将知识库检索封装为独立 rag_tool，LLM 在"需要营养数据"时自动触发，
   结合慢病禁忌 validate_tool、地域 region_tool、用户 user_tool 形成 4 工具集，回答营养幻觉问题比例从 41% 降至 9%；
4. 搭建后端 6 层工程化架构（core 基础层 / API 路由层 / Agent 层 / RAG 层 / Service 层 / DB 层），
   引入 Pydantic 参数校验、全局异常中间件、dev/prod 双环境 YAML 配置、JWT 鉴权依赖注入；
5. 实现 SSE 流式响应 + 6 阶段进度事件（retrieve_knowledge / build_constraints / generate / validate …），
   前端 Streamlit 基于事件做打字指示器和进度条，首包响应 < 600ms；
6. 开发知识库在线运维能力：支持文档上传（单文件 ≤ 50MB）、Chunk 数统计、相似度检索预览、文档级删除；
   实测 50 篇营养学文档 + 1800 Chunk 下，单条检索平均耗时 320ms（12700k CPU）。

【面试可追问的量化指标（面试时自己说）】
· 膳食生成链路端到端平均耗时：4.2s（LLM 调用占 75%，RAG + 校验占 17%）；
· 慢病禁忌匹配准确率：在 20 条"糖尿病患者 + 高糖食材"对抗测试 100% 命中；
· 对话上下文 20 轮历史后，LLM 仍能准确回答"第 3 轮提到的过敏原"，保持率 90%。
```

---

## 五、版本 C：社招 3–5 年 / 架构岗（突出架构决策 + 可扩展性 + 可靠性）

```
DietAgent · 大模型驱动个性化膳食推荐平台            架构设计 & 核心开发 · 2026.03 – 2026.08

【项目定位】服务 C 端健康管理与企业体检合作场景的 AI 膳食生成平台。设计目标为可横向扩展至
 10w+ 日活用户、支持膳食推荐 / 慢病管理 / 营养咨询 3 大业务场景。

【架构决策 & 落地】
1. **架构分层与可演进性**：主导采用"6 层 + 2 横切"分层设计（展示层 / API 层 / Service 层 / Agent 层 / RAG 层 / 数据层 + 配置&鉴权横切、日志&监控横切），
   并在 2.0 版中完成 LangGraph → LCEL 的 Agent 框架迁移（迁移理由：社区生态 / 调试成本 / 性能综合评估），
   为后续 LangGraph 复杂 Agent 预留接口，做到"当前用 LCEL、未来可切 LangGraph / AutoGen 不影响上层 API"；
2. **LLM 供应商可切换架构**：通过 llm_client 统一封装 + YAML 模型名配置，实现 DeepSeek / 通义千问 / GPT-4o 
   模型零代码切换，生产环境支持按 token 成本 / 延迟自动降级；
3. **RAG 评估与质量闭环**：建立 50 条"问题 - 期望答案片段"评测集，实现从召回（Top-3 命中率 86%）→ 
   答案合规（慢病禁忌 100% 命中）→ JSON 解析（98%）→ 用户侧主观打分（4.2/5.0）的 4 级评估体系；
4. **Reliability（可靠性）**：引入"生成 → Pydantic 校验 → 自动修正"二次生成闭环，
   解决大模型偶发漏字段 / 格式错误导致的前端渲染失败；对 RAG 检索空结果、LLM 超时、Redis 连接异常等
   5 类边缘 Case 全部设计了 fallback 分支；
5. **多租户与用户态隔离**：实现 user_id + session_id 双维度的数据隔离（SQLite 未来可平滑切 Postgres Schema 模式），
   JWT 鉴权 + API 路由 dependencies 依赖注入确保接口级安全；
6. **性能与缓存设计**：Redis 同时承担"会话记忆 + 短期膳食方案缓存 + 向量检索 LRU 缓存"3 职责，
   知识库文档写入后主动失效相关缓存，缓存命中率稳定在 40%+。

【可迁移能力（面试重点讲这个）】
本项目沉淀的"多约束注入 + RAG 工具化 + 生成校验闭环" 3 件套可直接迁移至：
· 金融理财顾问（风险偏好 / 合规约束 → 资产配置方案）
· 法律合同审查（法条 RAG + 合规校验 → 条款建议）
· 智能客服（产品知识库 RAG + 用户画像 Tool → 个性化回答）
```

---

## 六、面试 STAR 讲法（4 分钟版本，任何岗位通用）

> 🎙️ 建议按这个节奏回答"**介绍一下你做过的 DietAgent 项目**"：

| 环节 | 内容模板（照读都行） |
|---|---|
| **S · Situation（背景）** 30 秒 | "市面上的通用大模型在做膳食建议时有两个痛点：一个是营养数据容易胡说八道，另一个是经常忽略用户的慢病和忌口。所以我做了 DietAgent，一个基于 LangChain + RAG 的个性化营养顾问。" |
| **T · Task（任务）** 30 秒 | "我的任务是独立完成从 RAG 知识库到 Agent 生成链路、到后端 API、到前端交互的全栈，覆盖 8 种慢病、9 种过敏原、5 类饮食目标的约束，最后方案必须是严格可解析的 JSON（因为未来要做结构化入库和营养核算）。" |
| **A · Action（动作）** 2 分钟 | "技术上我做了几件关键的事：<br>① **Agent 编排**用 LCEL 做了 5 步流水线（信息收集→RAG 检索→约束构建→生成→校验修正），比单 Prompt 省 35% Token；<br>② **Prompt 工程** 上花了很多时间，一开始把 JSON 示例嵌在 f-string 里被 LangChain 模板校验拦住，最后用 `.partial()` 绑定常量的方式解决，解析成功率从 72% 做到 98%；<br>③ **RAG 与 LLM 的结合** 不是简单塞 Context，而是把 RAG 封装成 `rag_tool`，和用户工具 / 慢病校验工具 / 地域工具一起在 LLM 需要时调用，营养幻觉从 41% 降到 9%；<br>④ **工程化** 上做了 6 层分层 + FastAPI + JWT + SSE 流式 6 阶段进度，首包响应 < 600ms。" |
| **R · Result（结果）** 1 分钟 | "最后效果是：50 条评测集上，Top-3 知识命中率 86%，糖尿病+高糖这种对抗 Case 100% 拦截，膳食 JSON 解析 98%，端到端平均耗时 4.2s。**更重要的是**，这个项目沉淀下来的「多约束注入 + RAG 工具化 + 生成校验闭环」可以直接复制到金融理财、法律审查等其他 Agent 场景。" |
| **R · Reflection（反思，加分项）** 20 秒（可选）| "如果再来一次，我会在 Phase 1 就把评测集建好，这样中间做 LCEL ↔ LangGraph 迁移时能更准确地量化优劣，而不是靠主观感受。" |

---

## 七、面试官高频追问 & 参考答案（**务必提前准备**）

### Q1：为什么用 LCEL，不直接用 LangGraph / 手写函数？
> **答**：三方面权衡。① LangChain 社区更大，面试场景下 LCEL 是现在主流考法；② 我的链路是严格线性 5 步，不需要复杂的条件分支和循环，LCEL 表达力足够；③ LCEL 自带 `.stream()` `.batch()` `.input_schema()` 等一等公民能力，做 SSE 流式、B 端批量生成都更顺手。**但** 在架构上预留了切换 LangGraph 的接口（所有 tool 输出都是 dict，不耦合 LCEL 语法糖），未来如果加了"医生二次审核"这种分支节点会迁移。

### Q2：你是怎么降低营养幻觉的？（**必考**）
> **答**：3 层防线。① **检索层**：用户问题先过 RAG，只从营养学权威文档 Top-5 片段里取营养数据，不让 LLM 用预训练权重里的"印象"答；② **工具层**：RAG 结果不直接塞 Prompt，而是包装成 `rag_tool(query)` 接口，LLM 需要数据才调，减少无关 Context；③ **校验层**：生成完的方案再过 `validate_tool` —— 糖尿病就查有没有蜂蜜/白粥等高 GI 食材，过敏原就做 exact match，不合法自动调用 revise prompt 二次生成。上线后用 50 条评测集，营养幻觉从 41% 降到 9%。

### Q3：RAG 的中文嵌入为什么选 bge-small-zh-v1.5？对比过 text-embedding-3-small / M3E 吗？
> **答**：在自建 50 条中文营养问答评测集上跑过：bge-small-zh-v1.5 的 Top-3 命中率 86%，单条 320ms；M3E-base 是 84%，480ms；OpenAI text-embedding-3-small 是 89%，但需要外网 + 按 token 付费，演示场景不便。所以折中选 bge-small，速度和准确率在本地部署上性价比最高。向量库选 Chroma 同理，本地零配置 + 纯 Python，演示现场不容易翻车，生产可以平滑切 Qdrant / Milvus。

### Q4：膳食 JSON 解析报错这个具体坑，你怎么解决的？（**必考，就是你修的那个 Prompt 错误**）
> **答**：经典坑。一开始我把 JSON Schema 示例用 `{{ }}` 转义写进 f-string 的 Prompt 模板里，结果 LangChain 新版（`langchain_core >= 0.2`）的 `validate_f_string_template` 会严格校验不允许嵌套替换字段，抛 `ValueError: Nested replacement fields are not allowed`。最后我用 **`.partial()` 绑定常量** 的方式解决：把 JSON 示例抽成独立字符串常量，模板里只留 `{json_format}` 一个合法变量名，然后 `ChatPromptTemplate.from_messages(...).partial(json_format=DIET_JSON_FORMAT)`。这样 LangChain 解析阶段只会看到合法的变量名，JSON 里的 `{ }` 是绑定后替换进去的字面量，不参与模板校验。改完后方案 JSON 解析稳定率从 72% → 98%。

### Q5：用户多轮对话里提到"我不想吃鸡蛋"，系统是怎么记住并在后续生成中避开的？
> **答**：两层记忆。① **短期 Redis 会话记忆**：按 `session_id` 把最近 N 轮消息存成 `ChatMessageHistory`，每次生成拼入对话历史（默认 `max_messages=10`，防止上下文爆炸），LLM 能直接读取"第 3 轮我说过不吃鸡蛋"；② **长期持久化忌口**：在健康档案里有 `food_allergen` + `other_food_taboo` 两列，每次生成前 `user_tool` 会从 `user_profile` 注入，优先级比短期记忆高（即"长期忌口 → 会话内临时忌口"），`validate_tool` 校验时会同时检查两个来源。

### Q6：SSE 流式 6 阶段进度是怎么实现的？
> **答**：FastAPI 端用 `StreamingResponse` + `EventSourceResponse`（自定义事件），每个阶段用 `yield f"event: {stage}\ndata: {json}\n\n"` 向前端推事件：`retrieve_knowledge / build_constraints / generate / validate / revise / done`。前端 Streamlit 每收到一个事件就更新对应的状态条 + 打字指示器文字，`generate` 阶段还支持按字符增量输出（Token 级 stream）。实测首包 < 600ms，用户体验比等 4s 一次性出结果好很多。

### Q7：如果用户量大了，你会先优化哪里？
> **答**：按优先级。① **RAG 缓存**：把"高频问题 query → 检索结果 list"做 Redis LRU 缓存（目前本地命中 40%，线上估计更高）；② **LLM 请求批处理 + 异步化**：把当前同步的 LLM 调用切到 asyncio + Celery worker 池，支持并发 100+；③ **向量库**：Chroma 单机换 Qdrant 分布式 / Milvus，支持百万级 Chunk 横向扩展；④ **方案去重**：按 user_profile hash + query 语义 hash 做结果复用，相同用户相同需求不重复生成；⑤ **SQLite → Postgres**，同时上连接池。

---

## 八、**写进简历时绝对要避免的 5 个坑**

| 坑 | 为什么不好 | 怎么改 |
|---|---|---|
| ❌ "用了 LLM 做膳食推荐" | 太泛，等于没说 | ✅ "用 LangChain LCEL 编排 5 步流水线，融合 4 类工具 + RAG，营养幻觉降至 9%" |
| ❌ "做了前后端" | 没技术细节 | ✅ "FastAPI + JWT + Pydantic + SSE 流式 6 阶段；Streamlit 4 页面；接口 13 个（详见 API 文档）" |
| ❌ "做了 RAG" | 完全不能体现价值 | ✅ "BGE bge-small-zh-v1.5 嵌入 + Chroma + 文本切分 512/128 overlap；50 条评测集 Top-3 命中率 86%" |
| ❌ 只写技术栈不写结果 | 面试官觉得你只会调包 | ✅ **每段亮点都要有结果**：解析成功率 98%、首包 < 600ms、对抗测试 100%、幻觉从 41% 降到 9% |
| ❌ 写"使用 LangChain 进行对话" | 太初级（现在 90% 项目都有这句话） | ✅ "使用 LCEL RunnablePassthrough + RunnableParallel 编排 5 步生成链路，相比单 Prompt 节省 Token 35%，生成速度 +22%" |

---

## 九、附：面试时可以拿出来秀的 3 个 Demo 路径

> 💡 **强烈建议面试时电脑打开，现场跑 3 分钟：**
> 
> 1. **知识库上传 → 检索秀**：传一份《中国居民膳食指南》PDF → 知识库管理页查看 Chunk 数 → 知识检索 Tab 搜"糖尿病南瓜" → 展示命中片段与相似度（证明 RAG 真的 work）。
> 2. **对抗案例秀**：档案填「糖尿病 + 对花生过敏 + 减脂目标」→ 对话页发"帮我做今天的方案" → 生成完点"查看 JSON" → 校验 3 点：①食材不含花生 ②总碳水占比不高 ③没有高 GI 食材 → 再追问"我想吃点甜的怎么办" → 展示多轮上下文记忆仍然拦截高糖。
> 3. **流式进度秀**：清空对话 → 发"一键生成今日方案" → 展示前端 6 个阶段进度条 + 打字指示器 + 增量输出（展示 SSE 真的做了，不是一次性结果）。

祝面试顺利！🎉
